apiVersion: v2
name: trust-manager
description: Wrapper chart to deploy trust-manager and publish the platform root CA bundle.
version: 0.1.0
dependencies:
  - name: trust-manager
    version: "0.21.1"
    repository: "https://charts.jetstack.io"
