apiVersion: v2
name: oauth2-proxy
description: Platform-managed oauth2-proxy for protecting shared operator surfaces.
version: 0.1.0
dependencies:
  - name: oauth2-proxy
    alias: upstream
    version: "10.1.4"
    repository: "https://oauth2-proxy.github.io/manifests"
