---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: "{{ .Release.Namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
---
apiVersion: secrets.local.lab/v1alpha1
kind: KVMount
metadata:
  name: "{{ .Release.Namespace }}kv-mount"
  namespace: "{{ .Release.Namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec: {}
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: "{{ .Release.Namespace }}-secret-store"
  namespace: "{{ .Release.Namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "platform/{{ .Release.Namespace }}/kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "{{ .Release.Namespace }}"
          serviceAccountRef:
            name: "external-secrets"
            namespace: "{{ .Release.Namespace }}"
---
apiVersion: secrets.local.lab/v1alpha1
kind: Policy
metadata:
  name: "{{ .Release.Namespace }}-kv-policy"
  namespace: "{{ .Release.Namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  accessMode: readwrite
  engines:
    - kv
---
apiVersion: secrets.local.lab/v1alpha1
kind: AuthRole
metadata:
  name: "{{ .Release.Namespace }}"
  namespace: "{{ .Release.Namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backend: kubernetes
  roleName: "{{ .Release.Namespace }}"
  boundServiceAccountNames:
    - external-secrets
  policyNames:
    - "{{ .Release.Namespace }}-kv-policy"
  tokenTtl: 3600
