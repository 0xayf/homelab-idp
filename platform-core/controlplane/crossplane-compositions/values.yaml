# Crossplane Compositions Configuration
# These values are used by the compositions templates

# Provider configuration names (should match your Crossplane setup)
providerConfigs:
  vault: provider-vault-config
  kubernetes: provider-kubernetes-config

# SecretStore name for External Secrets integration
clusterSecretStore: vault-kv-store

# Default KV engine path in Vault
vaultKVPath: platform
